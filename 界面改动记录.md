# LRH 手机集群控制 APP · 界面改动记录

（2026-08-28 领导逐轮确认，swarm_mobile / app.py）

## 页① 全部飞机 —— 已定稿改动

1. 顶部固定栏整体压扁：连接行(IP/端口/连接/断开) 44→32dp、状态行 34→26dp、卫星行 84→80dp、字号 15→13sp；整栏总高 172→147dp。
2. 每架飞机格子改竖排两行：上=机号+颗数(机号红/颗数绿)，下=该机飞行模式(在线显示模式名·绿色，离线显示 `--`·灰色)。—— `_sat_box` 每块改为 vertical，`_refresh_satellites_impl` 重构。
3. 全队高度/全队速度两行满行均匀铺开：去掉右边空占位(原 0.06)，宽度改为 标签按钮 0.20 / 输入框 0.34 / 单位 0.12 / 按钮 0.34 = 1.0。
4. 三个标签(全队高度/全队速度/前后左右小组m)改为『切自动任务』式实心蓝圆角按钮(`_mk_btn`, ACCENT, 白字)；单位 m/m/s 改为实心红圆角按钮(DANGER, 白字)。原先的 GlassPanel 毛玻璃框已移除。
5. 前后/左右/小组m 标签按钮放大(size_hint_x 0.24→0.32, 字号 13→14sp)；三个间距输入框(5/5/10)缩小(size_hint_x 0.2→0.16)。
6. 移除毛玻璃 GlassPanel：bfm(全队高度/速度容器)、gr2(前后左右小组行)均已改普通 BoxLayout。

> 对应代码：`swarm_mobile/app.py` 的 `_build_ui` / `_build_fleet_ops` / `_build_formation` / `_refresh_satellites_impl`。

## 下一步

- 页② 单架飞机(`_build_single`)待改。
