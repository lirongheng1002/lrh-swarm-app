# LRH 手机集群控制 APP 使用说明

**LRH 手机集群控制 v1.0**（Android APP，Kivy 全中文界面）
与桌面控制台 **LRH集群控制 v2.0** 使用同一套飞行控制核心（MAVLink over 4G 云中继），
功能对齐：连接飞机、卫星数、单机/全队起降、单机/全队解锁上锁、模式切换、
自动执行任务、队形编队、任务航点设置/下载/上传、投弹指令。

---

## 一、它能干什么（对照清单）

| 功能 | 在 APP 哪里 |
|---|---|
| 连接飞机 | 顶部「服务器IP + 端口 + 连接」（默认 112.124.6.186 / 15551，每机一条链路，共 10 架） |
| 卫星数量 | 顶部卫星行：`机号:颗数` 横向滚动，绿色=在线，灰色=离线（GPS_RAW_INT） |
| 单架起飞 / 降落 | 「三、单机操作」选机后点「单机起飞[高度]/单机降落」 |
| 全队起飞 / 降落 | 「一、全队操作」全部起飞[起飞高度]/全部降落 |
| 全队解锁 / 上锁、单机解锁 / 上锁 | 「一、全队操作」全部解锁/全部上锁；「三、单机操作」单机解锁/单机上锁 |
| 模式开关 | 全部切模式（模式菜单）+ 单机切模式；全部切自动·各自任务 |
| 自动执行任务 | 「全部切自动·各自任务」= 各机切 AUTO 执行各自任务；单机也可切「自动」 |
| 队形编队 | 「二、队形编队」：全队高度、前/左右/小组间距、6 种预设（一字横排/人字形/前三角/后三角/梯形/三角群）、开始编队/暂停编队 |
| 任务航点设置/下载/上传 | 「四、任务航点」：经纬高输入「加航点」、「下载任务」（读飞机）、「上传任务」（写入飞机）、「清除任务」 |
| 航点后插投弹 | 任务表点选航点 → 「插投弹」（DO_REPEAT_SERVO 184，舵机6 PWM2000） |
| 投弹指令 | 「一」全部投弹；「三」单机投弹 |

**危险操作全部有二次确认弹窗**：全部解锁/上锁/起飞/降落/返航/投弹、单机各动作、上传任务。

---

## 二、装 APP（两种方式）

### 方式 A：GitHub Actions 云端打包（推荐，本机零安装）
1. 把本项目（`swarm-mobile` 整个目录）推到您的 GitHub 仓库（main 分支）；
2. 仓库 Actions 自动开始 `build-apk` 工作流，几分钟后产出 **LRH集群控制-apk** 工件；
3. 工件页下载 `*.apk` 传到手机安装（首次安装需允许"未知来源"）。
> 手动也可以：Actions → build-apk → Run workflow。

### 方式 B：本机 WSL2 打包
1. 安装 WSL2 + Ubuntu：`wsl --install -d Ubuntu`（装完按提示重启）；
2. 双击 `build_apk.bat`（脚本会自动在 WSL 内装 buildozer 并打包，首次下载 SDK/NDK 较慢）；
3. 完成后 APK 在 `bin\` 目录。

---

## 三、首次连接（真机使用）

1. 打开 APP；顶部填（已预填默认值，改了会自动保存到手机内 config.yaml）：
   - 服务器IP：`112.124.6.186`（阿里云 4G 中继）
   - 端口：`15551`（第1架=15551 … 第10架=15560）
2. 点「连接」→ 顶部显示 **已连接 · 在线 X/10**，卫星行显示每机颗数；
3. 飞机列表在「三、单机操作」的选机下拉里切换（1号机~10号机），右侧给出该机
   模式/高度/卫星/电压/解锁状态。

---

## 四、典型作业流程

1. **连接**：确认在线 10/10；
2. **解锁**：全地面安全 → 「全部解锁」（二次确认）；
3. **起飞**：「全部起飞」（起飞高度可改，默认 20m，各机升空）；
4. **队形**：「二、队形编队」选预设（如人字形）→ 「开始编队」：
   长机（1号机）自由飞，僚机按 全队高度+各自高度差 保持队形跟随（每 0.5s 下发引导目标）；
5. **各自任务**：到集合点后点「全部切自动·各自任务」，各机执行自己的任务航线；
6. **航点任务**（离线可先编辑）：输入 纬度/经度/高度 →「加航点」→ 重复加多个 →「上传任务」写入飞机；
   下载：点选机后「下载任务」，任务表显示航点；点选某航点 →「插投弹」在该点后插入投弹指令；
7. **返航/降落**：「全部返航」（RTL 回各自起飞点）或「全部降落」（紧急）。

> 与桌面控制台同一时刻**只用一个**控制飞机（避免两台同时下发指令冲突）。APP 与桌面用同一条 4G 中继链路，逻辑完全一致。

---

## 五、问题排查

- **连不上**：ping 服务器；检查阿里云安全组是否放行 15551~15560 对手机 IP；确认 4G 模块在线；
- **在线数少**：等 10~20 秒重连（自动退避重试）；确认各机 `SYSID_THISMAV` 与 1~10 对应；
- **没有卫星数**：GPS 未锁定（GPS_RAW_INT 无输出时显示 `--`）；
- **任务表空**：先「下载任务」（真机需已上传过任务）；
- **编队不动**：确认长机位置新鲜、僚机已切 GUIDED（开始编队会自动切）、飞机在线且有 GPS。

---

## 六、安全提醒

- 先小规模（2~3 架、低空、空旷）验证，再逐步到 10 架；
- 每架机独立设置失效保护（断链 RTL、低电、GPS 丢失）；
- 集合点各机错开高度防碰撞（编队高度 + 各机高度差）；
- 危险动作必须二次确认看清对象与数量再执行。